# 日志中心与接口目录

后台导航保留三个日志入口：操作审计、接口监控、小程序异常上报。接口监控通过访问/错误页签切换；4xx 与 5xx 均在错误页呈现，并保留已脱敏的错误请求快照。

操作审计记录后台页面进入。相同管理员、相同模块在 30 分钟内仅保留一条 `ADMIN_MODULE_VIEW`，避免菜单点击制造噪声。

接口目录由后端路由注册表同步，默认生成业务模块、功能名称与用途说明；已有管理员填写的描述绝不覆盖。列表展示 24 小时调用量、错误量与最后调用时间。访问与错误日志返回可读的操作人标签，页面不展示内部 CUID。
